# 验证记录

## 0.4.0 茶舍体验版 · 2026-09-20

- 使用 Unity 6.6 随附的 C# 编译器及现有依赖编译 7 个程序集：规则、资源、应用、界面、编辑器、两组测试，均无编译错误；存在既有 FindFirstObject 等 API 弃用警告。这是编译检查，不是 Unity 播放模式通过。
- 使用独立 .NET 8 规则程序运行 402 项断言，验证全部八种首幕选择、4 当量配方、门灯资金可达性、旧余额、未读回应、写失败回滚和重复交付。该程序直接编译项目 Domain 源码，存储使用独立内存实现，不验证 Unity JsonUtility。
- 更新原有测试并新增自由停留不改资源、可取消端茶与自然倒水完成测试；计划 45 项 EditMode、9 项 PlayMode，尚未运行。
- Unity 启动返回 198：没有可用 entitlement，因此新画面、声音、触控及构建无法验收。原 0.3.0 的 52 项测试及截图不代表本版通过；本机已有 app 仍是 0.3.0。
- 无新版截图或声音试听结论。恢复许可证后执行准备场景、两组测试、开发构建和 `--yijing-smoke-output`，再进行耳机与扬声器试听。


## 0.3.0 故事与引导版 · 2026-09-20

Unity 6000.6.0f1；使用独立工程副本和隔离存档验证，不关闭用户编辑器、不清空原进度。

| 检查 | 结果 |
| --- | --- |
| EditMode | 45 通过，0 失败；含故事映射、动态提示、点灯前置、引导状态事务和真实 schema=1 文件迁移 |
| PlayMode | 7 通过，0 失败；含静／静／行与全行两条完整首幕、指针拖拽、恢复操作、合成路径和独立练习退出 |
| 界面文字 | 自动检查开场、三段回应、结尾、帮助与手账文字容器；修复结尾及帮助页裁切 |
| macOS 开发构建 | 0.3.0 构建成功；独立运行完整三单、点灯、送别和卦卡，输出 `orders=3`、`oracle=001_000` |
| 实际画面 | 检查合成高亮、分支回应、结尾与卦卡截图，路径保存在 Artifacts/onboarding-preview |
| 存档兼容 | 保留 schema=1 的物品、灵石、进度和日记录；历史故事选择未知时保持空白，独立练习不写正式存档 |

验证时序：52 项测试通过后，结尾补充“故事三次回应与每日前三单分别记录”的说明，该版本已构建并完成独立运行截图。画面检查后又做两处纯文案修整：缩短结尾第一句以改善换行，将 46 灵石“正好”改为“足够”支付 40。最终再次启动编辑器复测时，本机授权返回 198（没有可用 entitlement），未进入测试或构建。因此源码包含这两处文案修整，现有 app 仍是修整前已验证构建；授权恢复后需重新执行 PlayMode 和构建。不能把这次未启动的复测计为通过。

本轮范围是首幕 E01–E03，不是十八单完整内容或商业留存验收。新玩家是否无需解释就能完成首单、满盘能否自行恢复、是否理解故事因果，仍需按 STORY_ONBOARDING.md 做观察试玩。手机触控、安全区和设备性能尚未验收。

## 0.2.0 首段可玩版 · 2026-09-19

Unity 6000.6.0f1，macOS Apple Silicon。使用工程独立副本验证，未关闭用户的编辑器。所有自动玩法测试使用临时存档。

| 检查 | 结果 |
| --- | --- |
| Unity 编译与 Game 场景装配 | 通过，配置／ArtCatalog／中文字体引用有效 |
| EditMode | 37 通过，0 失败，含原有 15 项配置测试及 22 项规则／存档测试 |
| PlayMode | 4 通过，0 失败；含指针拖拽、越界取消、储物回收撤销、三单交付／点灯／重新加载 |
| 中文布局回归 | 首次进入、请求变体、卦卡页面的可见文字高度均不超过容器 |
| macOS 开发构建 | 成功；窗口模式，540×960 参考布局 |
| 独立运行 | 自动完成 E01–E03，静／静／行得到 `001_000`、第 15 卦谦 |
| 画面检查 | 实际运行截图检查茶案、交付清单、六爻方向与卦卡正文；修复标题和阶数裁切 |

规则测试覆盖资源当量、实例唯一性、满盘恢复、五阶终点、储物授权、陈旧输入、重复交付、写入失败回滚、回收撤销、跨日与时钟回拨、常客轮换持久化、备份恢复及损坏文件保留。界面测试还验证完成三单和点灯后剩余 38 灵石，重新打开状态一致。

原始 XML、构建日志、实际截图与本机 app 保存在 `Artifacts/`，不提交 Git。Development 构建可通过 `--yijing-smoke-output <目录>` 执行隔离存档的截图流程；普通启动不会执行该流程，正式非 Development 构建不会包含这段辅助代码。

本轮不是完整发行验收：未测 Android／iOS、手机多指触控、安全区、实际玩家手感与设备性能；未完成声音、英文、图片导出、完整十八单和全部修葺。基础库存与存档完整性通过不代表长期经济与留存已验证。

## 0.1.0 初始化历史

日期：2026-09-19。工程版本：0.1.0。编辑器：Unity 6000.6.0f1（f7f8ed4d1e24），macOS Apple Silicon。

| 检查 | 结果 |
| --- | --- |
| 官方 2D 模板依赖还原与工程脚本编译 | 通过 |
| 配置导入 | 10 物品、2 生产台、18 订单、6 故事节点、6 修葺、8 卦卡 |
| EditMode | 15 通过，0 失败，0 跳过 |
| PlayMode | 1 通过，0 失败，0 跳过 |
| Bootstrap → Game | 成功进入 Game，主相机存在且为正交相机 |
| Unity .meta 完整性 | Assets 中全部文件与目录均有对应元文件 |
| 包版本 | manifest 与 packages-lock 已保存 |

检查包含：升级链跨链错误、重复 ID、未知材料、静行成本不等、奖励不符、故事引用缺失、故事／修葺循环依赖、卦序映射错误、缺失卡片、缺失规则；配置快照隔离与序章必经修葺的资金可达性也已验证。

测试通过批处理无图形模式执行，验证编译、配置与启动生命周期，不等于正式画面、触控或性能已经验收。当前没有棋盘、订单交付 UI、玩家存档或完整可玩循环。

尚未验证：Android/iOS 构建与真机启动、保存恢复、触控、安全区、渲染表现、音频与设备性能。本机初始化时缺少 Android/iOS Build Support，M0 仍有待完成事项。

复现入口见 README。原始测试 XML 和编辑器日志保存在本机 Artifacts 目录，不提交到 Git；本记录用于保留初始化结论。
